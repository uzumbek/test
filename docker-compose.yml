services:
  portainer-test:
    build:
      dockerfile: Dockerfile
    volumes:
      - /var/lib/docker/volumes/portainer_data/_data/compose/4/src:/usr/src/app/src
    container_name: portainer-test
    environment:
      - TEST=env_value_test

  
